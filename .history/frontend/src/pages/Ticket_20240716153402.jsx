import { useDispatch, useSelector } from "react-redux";
import { getTicket, reset } from "../features/tickets/ticketSlice";

useSelector useDispatch getTicket reset

function Ticket() {
	return <div>Ticket</div>;
}

export default Ticket;
